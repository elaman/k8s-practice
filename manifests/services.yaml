apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace : drupalhosting
spec:
  selector:
    app: web
    env: dev
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000